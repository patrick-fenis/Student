<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fruits App</title>
</head>
<body>
  <h1>Fruits Index Page</h1>
  <nav>
    <a href="/fruits/new">Create a New Fruit</a>
  </nav>
  <% const myName = 'Hunter' %>
  <ul>
    <% for(let i = 0; i < fruits.length; i++){ %>
      <li>
        The <a href="/fruits/<%=fruits[i]._id%>"><%=fruits[i].name%></a> is <%=fruits[i].color%>,
        <% if(fruits[i].readyToEat === true){ %>
          It is ready to eat 
        <% } else { %>
          It is not ready to eat 
        <% } %>
        <form action="/fruits/<%=fruits[i]._id%>?_method=DELETE" method="POST">
          <input type="submit" value="DELETE" />
        </form>
        <a href="/fruits/<%=fruits[i]._id%>/edit">Edit</a>
      </li>
    <% } %>
  </ul>  
  <h3> Created by: <%= myName %></h3>
</body>
</html>